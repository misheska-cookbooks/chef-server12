box: box-cutter/chefdk
build:
  steps:
    - script:
        name: run chefspec
        code: /opt/chefdk/embedded/bin/rspec --color
    - script:
        name: run foodcritic
        code: foodcritic . --exclude spec
    - script:
        name: run rubocop
        code: rubocop
